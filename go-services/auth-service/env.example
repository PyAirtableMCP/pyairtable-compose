# Auth Service Configuration
# Copy this file to .env and update the values

# Server Configuration
PORT=8001
ENVIRONMENT=development

# Database Configuration
DATABASE_URL=postgres://postgres:password@localhost:5432/pyairtable_auth?sslmode=require

# Redis Configuration
REDIS_URL=redis://localhost:6379
REDIS_PASSWORD=

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# CORS Configuration
CORS_ORIGINS=http://localhost:3000,http://localhost:3001

# Timeout Configuration
REQUEST_TIMEOUT=30s
SHUTDOWN_TIMEOUT=10s

# Production Settings
# For production, make sure to:
# 1. Use a strong, random JWT_SECRET (at least 32 characters)
# 2. Set DATABASE_URL with proper SSL settings
# 3. Configure REDIS_PASSWORD if Redis requires authentication
# 4. Set CORS_ORIGINS to your actual frontend domains
# 5. Set ENVIRONMENT=production